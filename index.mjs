import kenyaTowns from './index.js';

export const {
  getAll,
  search,
  getByName,
  getByCounty,
  getByConstituency,
  getByType,
  getSatelliteTowns,
  nearBy,
  listCounties,
  query,
} = kenyaTowns;

export default kenyaTowns;
